use (comment) as a nop
agents for irc?
make replyFn take 2 arguments
use commute instead of alter when possible
change irc queue to an atom, use swap! to clear it
update readme
use clj-json instead of cheshire?
provide defaults from (get) where applicable
  remove :defaultAllow in permSet stuff
  fix allowed to do something if permSet does not exist
